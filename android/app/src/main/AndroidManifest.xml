<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Location is read only to place the child in a ~0.1° cell for the
         rarity scale (NFA-08), never tracked. ACCESS_BACKGROUND_LOCATION and
         FOREGROUND_SERVICE_LOCATION went with survey mode's GPS tracking in
         transition step 0.6 — for a children's app, a background-location
         permission is one you do not want to have to justify to a reviewer.
         ACCESS_FINE_LOCATION is still declared because Geolocator needs it to
         reach GPS at all; the app requests only coarse accuracy. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <!-- screenOrientation is "fullUser", not the default "unspecified":
             phones leave the 180° reverse-portrait position out of sensor
             rotation, so the app would never turn upside down even with
             auto-rotate on. Users aim the bottom microphone upward by
             inverting the phone, so we need that position. Deliberately not
             "fullSensor", which would also rotate for users who have
             auto-rotate locked off. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- The audio ACTION_SEND / ACTION_VIEW filters went with File
                 Analysis (transition 0.6). They advertised the app as a target
                 for "share an audio file with…", and the Dart side that handled
                 that hand-off no longer exists — so the app would have appeared
                 in the share sheet and then done nothing. A children's app has
                 no business being a general audio import target either. -->
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Work around rare Adreno Vulkan driver crashes in Flutter Impeller
             during offscreen image cleanup by using Skia on Android. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Notification small-icon for the foreground service.
             Referenced by NotificationIcon(metaDataName: ...) in
             lib/shared/services/background_recording/background_recording_service.dart
             (and, until they are removed, by the legacy survey and ARU
             notification services). -->
        <meta-data
            android:name="com.birdnet.live.notification_icon"
            android:resource="@drawable/ic_notification" />
        <!-- Foreground service for background recording. Type is "microphone"
             only: background recording listens, it does not track position
             (transition 0.6). Not wired up yet — see
             lib/shared/services/background_recording/. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="microphone"
            android:stopWithTask="true"
            android:exported="false" />
        <!-- Home-screen "Quick Listen" widget. See QuickListenWidgetProvider.kt. -->
        <receiver
            android:name=".QuickListenWidgetProvider"
            android:exported="false"
            android:label="@string/quick_listen_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_listen_widget_info" />
        </receiver>
        <!-- Compact 1x1 "Quick Listen" widget. Same click behavior, smaller
             icon-only layout. See QuickListenWidgetProvider.kt. -->
        <receiver
            android:name=".QuickListenWidgetProviderCompact"
            android:exported="false"
            android:label="@string/quick_listen_widget_1x1_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_listen_widget_1x1_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required so url_launcher's canLaunchUrl / launchUrl can resolve
             external https/http links (e.g. species Wikipedia/eBird/iNaturalist
             chips, About-screen links). Without these <intent> entries Android
             11+ package visibility hides browsers from us and url_launcher
             reports "no activity found", even when Chrome/Firefox are installed. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
